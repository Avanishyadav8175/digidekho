export const metadata = {
  title: 'Admin Login - Digital Dekho',
  robots: {
    index: false,
    follow: false,
  },
};

export default function AdminLoginLayout({ children }) {
  return children;
}